[package]
name = "settings-core"
version = "0.1.0"
edition = "2021"

[dependencies]
automation-core = { path = "../automation-core" }
mcp-core = { path = "../mcp-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"

[dev-dependencies]
tempfile = "3"
