{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Voice App desktop shell.",
  "windows": ["main", "overlay", "result"],
  "permissions": ["core:default"]
}
